#!/bin/bash
# Clean cache of old files
# Number of days to keep
readonly DAYS=7
. "$(dirname "$0")"/setup-vars

rm -f "$HOME/ROOT/static/reports/"tmp.*
exec find "$XDG_CACHE_HOME"/testclutchlogs "$XDG_CACHE_HOME"/curldaily -xdev -depth -type f -ctime +"$DAYS" -delete
